<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Page Commune!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        <link rel="stylesheet" href="https://bootswatch.com/5/cerulean/bootstrap.css">
        {% block stylesheets %} {% endblock %}
    
        {% block javascripts %}{% endblock %}
      
    </head>
    </head>
    <style>
 .navbar{
  position:fixed;
  width: 100%;
  top:0px;
  margin-bottom: 30px;
  background-color: #3A5154;
  
}
container{
  background-color: #929AA2;
 
}
     
    </style>

   
  <nav class="navbar  navbar-expand-lg navbar-dark" style=" background-color: #3A5154">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('blog1')}}">GlobeNature</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item active">
          <a class="nav-link" href="{{ path('blog')}}">Articles
            <span class="visually-hidden">(current)</span>
          </a>
        </li>
         {% if app.user %}
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ path('app_logout')}}">A Bientot</a>
          </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('article_new')}}">Rediges ton Articles</a>
        </li>
        {% else %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_login')}}">connexion</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_register')}}">Enregistrement</a>
       {% endif %}
      </ul>
      {% if app.user %}
      <li class="nav-item">
        <a href="" class="nav-link" style="color:azure; font-weight:bolder;" >Bienvenue {{app.user.prenom}} </a>     
       </li>
       {% endif %}
    </div>
  </div>
</nav>   
<div class="container">
        {% block body %}
        
        {% endblock %}
      </div>
 
    </html>
      